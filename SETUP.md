# Eventura - Инструкция по настройке

## Текущий прогресс

### ✅ Выполнено:
1. **JSON файлы с данными** - созданы в `stubs/data/`:
   - `users_and_vendors.json` (100 записей)
   - `services.json` (205 услуг)
   - `vendor_profiles.json` (70 профилей)
   - `events.json` (100 мероприятий)
   - `bookings.json` (100 бронирований)
   - `reviews.json` (100 отзывов)
   - `event_types.json`

2. **API Endpoints** - созданы в `stubs/api/eventura/`:
   - `/api/eventura/vendors` - получение подрядчиков
   - `/api/eventura/services` - получение услуг
   - `/api/eventura/events` - работа с мероприятиями (CRUD)
   - `/api/eventura/bookings` - работа с бронированиями
   - `/api/eventura/ai-search` - ИИ-подбор через GigaChat

3. **Настройка проекта**:
   - Проект переименован в `eventura`
   - Обновлена конфигурация `bro.config.js`
   - Создана главная страница (HomePage)
   - Обновлен Header
   - Обновлен роутинг

### 🔄 В процессе:
1. Создание страниц (BookingPage, VendorProfilePage)
2. Обновление CatalogPage для Eventura
3. Обновление ChatPage для Eventura
4. Создание CSS файлов

### ⏳ Осталось:
1. Завершить создание всех страниц
2. Интеграция с API на фронтенде
3. Добавить CSS стили
4. Тестирование

## Настройка .env

Создайте файл `.env` в корне проекта со следующим содержимым:

```
GIGA_AUTH=MDE5YTFhYzUtMTdhYy03MGIyLTk1MmQtN2Y5ZGI0YzFjODZhOmZjZTI2NjVlLThmZWUtNDU5Ni04YTVhLTQxOTE0MjE1OGY4OQ==
```

## Запуск

```bash
npm ci
npm start
```

Проект запустится на http://localhost:8099/eventura

## Структура API

Все API endpoints доступны через `/api/eventura/*`:
- `GET /api/eventura/vendors` - список подрядчиков
- `GET /api/eventura/vendors/:id` - детали подрядчика
- `GET /api/eventura/services` - список услуг
- `POST /api/eventura/events` - создать мероприятие
- `POST /api/eventura/bookings` - создать бронирование
- `POST /api/eventura/ai-search` - ИИ-подбор


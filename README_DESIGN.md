# 🎨 Eventura Design System - Обновления

## Что было сделано

### ✅ 1. Создана полноценная дизайн-система
- **Chakra Theme** (`src/theme/index.ts`) с брендингом:
  - Единая цветовая палитра (pink как primary)
  - Типографика (Inter)
  - Кастомные стили для всех компонентов
  - Поддержка dark mode

### ✅ 2. Почищен global.css
- Удалены все хаки на сгенерированные Chakra классы
- Восстановлена accessibility (focus states)
- Оставлены только базовые reset стили

### ✅ 3. Обновлен Header
- Sticky вместо fixed (правильный layout)
- Адаптивное мобильное меню (Drawer)
- Active states для навигации
- Dark mode toggle
- Красивые hover эффекты

### ✅ 4. Созданы shared компоненты
- `PageShell` - обертка для страниц с правильными отступами
- `EmptyState` - для пустых состояний
- `ErrorState` - для ошибок
- `LoadingState` - для загрузки

### ✅ 5. Заменены все alert() на useToast
- Красивые toast уведомления
- Единый стиль по всему приложению
- Правильные типы (success, error, info, warning)

### ✅ 6. Исправлен Loader
- Chakra Spinner вместо inline styles
- Правильная анимация
- Консистентный дизайн

### ✅ 7. Созданы переиспользуемые UI компоненты
- `VendorCard` - карточка подрядчика
- `ServiceCard` - карточка услуги
- `StatCard` - статистика

### ✅ 8. Унифицированы цвета
- Pink = primary (все основные действия)
- Blue = secondary/info (информационные кнопки)
- Единый стиль по всему приложению

### ✅ 9. Улучшена accessibility
- Правильные focus states
- aria-labels везде
- Keyboard navigation
- Семантический HTML

### ✅ 10. Исправлен Suspense fallback
- Красивый LoadingState вместо "Loading..."

## Структура новых файлов

```
src/
├── theme/
│   └── index.ts              # Chakra theme с брендингом
├── components/
│   ├── shared/
│   │   ├── PageShell.tsx     # Обертка для страниц
│   │   ├── EmptyState.tsx    # Пустые состояния
│   │   ├── ErrorState.tsx    # Ошибки
│   │   ├── LoadingState.tsx  # Загрузка
│   │   └── index.ts
│   └── ui/
│       ├── VendorCard.tsx    # Карточка подрядчика
│       ├── ServiceCard.tsx   # Карточка услуги
│       ├── StatCard.tsx      # Статистика
│       └── index.ts
└── hooks/
    └── useToast.ts           # Хук для toast уведомлений
```

## Как использовать

### Theme
Theme автоматически подключен в `App.tsx`. Все компоненты используют единый стиль.

### Shared компоненты
```tsx
import { PageShell, EmptyState, LoadingState } from '../components/shared'

// В странице
<PageShell>
  <EmptyState 
    title="Нет данных"
    description="Добавьте что-то"
    actionLabel="Добавить"
    onAction={handleAdd}
  />
</PageShell>
```

### UI компоненты
```tsx
import { VendorCard, ServiceCard } from '../components/ui'

<VendorCard
  vendor={vendor}
  isFavorite={isFavorite}
  onToggleFavorite={handleToggle}
/>
```

### Toast уведомления
```tsx
import { useToast } from '../hooks/useToast'

const { showSuccess, showError, showInfo } = useToast()

showSuccess('Успешно!', 'Операция выполнена')
showError('Ошибка', 'Что-то пошло не так')
```

## Dark Mode

Dark mode включен и работает через toggle в Header. Theme автоматически переключает цвета.

## Что дальше можно улучшить

1. Добавить больше анимаций (framer-motion уже подключен)
2. Создать больше переиспользуемых компонентов
3. Добавить storybook для компонентов
4. Оптимизировать bundle size
5. Добавить PWA поддержку

